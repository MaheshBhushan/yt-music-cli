{
  "devDependencies": {
    "wrangler": "^4.133.0"
  },
  "name": "ytm-verification-site",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  }
}
